# Event log — First Commit (Sep 17–20, 2026)

## Day 1 — Thu Sep 17

- **08:26** — Live site check: countdown flipped to "ENDS IN 3 DAYS, 11 HOURS" → build clock open; deadline ≈ Sun ~19:30 IST (schedule page still says exact hours are being finalised).
- **08:28–08:29** — Repo created at kickoff; scaffold committed (docs, license, README, .gitignore).
- **08:31** — Frontend scaffolded (Vite 8 + React 19 + TS), builds clean; committed.
- **08:33** — Sim engine skeleton committed: decode physics (bandwidth-bound) + fit accounting; **20 tests passing**, calibrated to three measured anchors (A1: RTX 3090 → 111.74 tok/s; A2: RTX 4090 → 127.74; A6: M2 Ultra → 76.28) — each within its published tolerance. A negative-control test proves the tolerance band rejects uncalibrated physics.
- **08:35** — Lambda `clusterbreak-api` (python3.13) live — direct invoke returns the `/health` payload (200).
- **08:38** — API Gateway HTTP API created; `GET /health` verified over the public endpoint (`https://wa7rwqxhk0.execute-api.ap-south-1.amazonaws.com/health` → `{"ok": true, ...}`).
- **08:38–08:40** — Frontend synced to the private S3 bucket; CloudFront `EX9Y84FE8SFH3` deployed with OAC + bucket policy; landing page live over HTTPS.
- **08:40–08:42** — Verification sweep: CloudFront 200 (SPA fallback → 200 on unknown paths; direct S3 object → 403, i.e. bucket is private), API `/health` → 200. Commits `9858f63`, `d21c698`.
- **08:41** — Submission form not visible on the event site yet (nav shows only "Check in"); re-check before end of day.
- **09:59–10:17** — **Interactive 3D rig builder live**: place devices on the 3D grid board (palette → board), drag/snap to cells, wire nodes into pipelines (link mode), live decode + pipeline estimates with fit verdicts per node and for the rig as a whole. Engine gained the pipeline estimator — anchors A3 (3090×2 → 108.07 tok/s) and A4 (70B ×2 → 16.29) now covered, **25/25 engine tests**. Frontend rig logic covered by 9 unit tests.
- **10:15** — Deployed-site verification (scripted browser, software GL): WebGL renders, three nodes placed with correct estimates (RTX 3090 + Llama-3.1-8B Q4_K_M → 111.7 tok/s = anchor A1), model/quant/context switches recompute live, **zero console errors**. Screenshot: `/tmp/cb-shot.png`.
- **10:22** — Interaction verification on the deployed bundle: LINK MODE wires two nodes and the pipeline estimate appears ("pipelined across 2 nodes · 1 GbE assumed"); dragging a node moves it across cells; zero console errors through the whole sequence. Screenshot: `/tmp/cb-shot2.png`.
- **10:29** — **Run simulator engine**: deterministic token loop (KV grows with context), fault injection (unplug a node, throttle a link), death detection with repair lists — 9 new tests, engine at **34/34**.
- **10:31–10:36** — **Break-it mode live**: RUN button, live VRAM meters on each node, event console over the board, UNPLUG / link-throttle fault controls, and a postmortem card ("what died and why" + repairs + event trail).
- **10:36** — Verified on the deployed bundle (scripted browser): Llama-3.3-70B across two wired RTX 3090s ran at 13.1 tok/s → unplugging one node killed it with the exact cause (survivors need 43.56 GB, have 24 GB) + repairs; throttling the link 1 → 0.01 GbE moved transfer time 0.13 → 13.11 ms/token live; **zero console errors**. Screenshots: `/tmp/cb-shot3.png` (postmortem), `/tmp/cb-shot4.png` (throttled run).
- **Data pipeline (built after 10:36; see clock note)** — `tools/refresh_data.py` now regenerates the whole dataset from live sources: measured benchmark tables (15 decode + 15 prompt rows), exact GGUF sizes from the Hugging Face API, and **15 device efficiencies fitted from measured runs**. It immediately killed an estimate: Qwen3.5-35B-A3B Q4_K_M was 20.5 GB "estimated" in the research — the live source says **22.29 GB**. The engine is now data-driven (JSON → engine), and devices without a measured anchor are marked unverified in-app instead of showing a default-parameter number. 34/34 engine tests still green; `docs/data-report.md` documents method, sources and gaps.
- **~06:00** — **Custom rig + live model library + hardware detection**: (1) `DETECT THIS MACHINE` reads the browser's GPU renderer and matches the catalog (or prefills the custom form); (2) `tools/detect_hardware.py` probes the local machine (nvidia-smi/CPU/RAM) and its JSON imports straight into the app — verified end-to-end with this machine's own probe (RTX 5060 Laptop 8GB → catalog match → placed on the board); (3) custom devices accept an owner-measured tok/s and are calibrated with the exact pipeline formula; (4) MODEL LIBRARY searches Hugging Face live and pulls a model in with exact blob sizes + architecture resolved via the base-model → config.json chain (unsloth mirror for gated bases). Verified live: bartowski Llama-3.1-8B added with arch verified + 4.92 GB. 37/37 engine tests (3 new calibration tests).

- **~06:10** — **Claim grader + verification suite**: `tools/grade_claims.py` (recompute-from-sources + live re-fetch + suite runs + negative controls) scores the shipped claims — first run **12/13** with the single failure being a *grader self-bug* (regex caught a TypeScript type line, and the OOM-bounds pattern matched the wrong assertion); fixed the instrument, re-ran → **13/13, controls 3/3 caught**, report in `docs/grade-claims.md`. Browser verification scripts moved into `tools/browser/` (5 scripts). Two reusable patterns captured as skills: `headless-webgl-verification`, `live-data-pipelines`.

- **~06:45** — **Presets + verdict card**: five one-click rigs (This laptop · RTX 5060, Two laptops · 5060+3050, Classic homelab · 2×3090, Mac Studio · M2 Ultra, Steam Deck) with `?preset=<id>` deep links for sharing. The **verdict card** (right panel, copyable) summarises any rig: hardware, model + verified footprint, throughput (pipelined or per-node), fit verdict, the **KV-death context** from a new `predictOomContext` engine function (cross-checked in tests against the run simulator's own death point within 2%), and a weakest-node unplug prediction (run dies vs survivors continue). Building it immediately caught a real defect: the dual-3090 preset *ran* at 13.2 tok/s while the fit line said "does not fit" — two accounting conventions had drifted apart (a flat 12%-of-memory usable rule vs the simulator's 0.5 GB/device reserve). Unified the convention — capacity = memory − 0.5 GB; footprint = weights + KV — which makes the aggregate fit check and the per-node run rule algebraically the same statement, and (importantly) stops the app from declaring 2×3090 "cannot fit" a 70B Q4 that the measured anchors run. 41/41 engine tests (4 new predictor tests), 9/9 rig tests; live-verified on the deployed bundle: deep link fills the board, presets rebuild it, 13.2 → 51.5 tok/s across preset switches, zero console errors. One deploy hiccup was the CDN serving the previous index during invalidation (transient, re-verified clean — logged so it isn't mistaken for an app bug later).

- **~07:05** — **Shareable postmortems (runs API v0.2)**: `POST /runs` → DynamoDB (`clusterbreak-runs`, on-demand + 90-day TTL) → `GET /runs/{id}`; random ids, no auth, bounded validated payloads (≤8 KB). Frontend: SHARE REPORT on the postmortem card → link appears → `/?report=<id>` renders a read-only report page (rig, model, throughput, the verdict card, honest data note, CTA). **Verified end-to-end live**: unplug → postmortem → share → `https://d1at2woaiwy2hz.cloudfront.net/?report=1jyqtc3t1r` renders on a fresh page; zero console errors both; 9/9 API battery (incl. negative cases), 12/12 frontend tests. Two bugs caught by distrusting green: (a) module-level `_table` shadowed by `def _table()` (accessor returned itself); (b) **boto3/DynamoDB rejects Python floats** — silent 503 on every POST. The decisive move was running the *deployed* handler locally against the real table (uv + boto3) after CloudWatch gave nothing; found `TypeError: Float types are not supported` in seconds. Fix: `parse_float=Decimal` in, `_json_default` out.

- **~07:40** — **Deploy kit → AWS** (the pivot, bounded): from the rig on the board, Clusterbreak now generates a **real, deployable CloudFormation template** — VPC + public subnet + IGW + SG + one EC2 node per NVIDIA device (LaunchTemplate per node so spot works), llama.cpp served via Docker in userdata (CUDA path + CPU path), model pulled from the Hugging-Face-verified repo/file in the dataset. Live costs from the AWS Pricing API (vendored snapshot + `tools/refresh_pricing.py`, timestamped in the UI; spot noted as a moving snapshot). Honest boundaries stated in-UI: cloud silicon ≠ consumer cards (tok/s differ), unified-memory/laptop parts aren't on EC2 and are listed as *skipped*, G-instance quota required (this account's quota was 0 → **increase requested for on-demand + spot**, pending). Verification chain: 17/17 frontend tests → deployed → downloaded the real template from the live site → **cfn-lint: 0 errors** (it caught two real bugs on the way: `InstanceMarketOptions` isn't a property of `AWS::EC2::Instance` — moved into a LaunchTemplate; and an em-dash in `GroupDescription` violating the allowed charset). cfn-lint's W2001 also exposed a *critical* one: the unbraced-`$VAR` bash design meant CloudFormation never injected `ModelUrl`/`GpuMode` into userdata — fixed with `${Param}` references (the whole point of linting the artifact, not trusting it). Next: live deploy verification (CPU path on a t3 while G-quota approves), then the writeup.

- **~08:05 (Sep 20)** — **Persistent sessions + chat with your own rig — verified against a real g5.xlarge.** (1) **Sessions persist** (localStorage) and the **rig list is read back from AWS** on every load (`GET /aws/stacks/{session}`), so a refresh — or another browser after reconnecting — shows every running rig with status, endpoint, teardown timer, and a **tear down** button. (2) **Chat is real**: `POST /aws/chat` proxies to the rig's llama.cpp server with the per-stack bearer key (the browser can't call `http://…:8080` from HTTPS, and the key stays server-side). Verified live in the UI: *"I'm running on an Amazon EC2 instance provisioned by Clusterbreak."* — 1.4 s round trip, zero console errors, then **torn down from the UI** (no instances left, billing stopped). (3) **Harness use**: the drawer reveals the rig's `cbk-…` key and a paste-ready `OPENAI_BASE_URL`/`OPENAI_API_KEY` block for any OpenAI-compatible client; port 8080 is open **because the key is the gate**. (4) **Five real bugs found only by live deployment** (each invisible to unit tests and curl): the connect role couldn't resolve the Deep Learning AMI parameter (`ssm:GetParameters` on `/aws/service/canonical/*` only) → GPU launches failed AccessDenied; the 60 GB root volume was smaller than the DLAMI's 75 GB snapshot; a hardcoded launch-template name collided with leftovers from a failed attempt; the DLAMI's preinstalled `containerd.io` conflicts with `docker.io`, and under `set -e` that aborted the whole bootstrap before the model download; and **our own auto-teardown sweep deleted a freshly re-created rig** because a stale session record still owned the stack name (fixed with a claim transfer, a claim release on teardown, and a `CreationTime > teardownAt` guard in the sweep). Plus one that only a browser could catch: the new listing route returned a bare dict instead of `_response(...)`, so it shipped without CORS headers — curl was happy, the browser blocked it. GPU bootstrap now uses the **AWS Deep Learning base AMI** (driver + toolkit preinstalled: nothing to install, nothing to restart), the unit retries and logs to the EC2 console, and the template is cfn-lint clean.
- **~02:10 (Sep 20)** — **UI v2: dynamic + colourful + the AWS connect UI, shipped.** (1) **Overlay bug fixed** — the postmortem report was being painted over by the 3D node labels (drei's `<Html>` portal sits at z-index 20; the backdrop had none). `.postmortem-backdrop` now stacks at 40, verified with `elementFromPoint` at a point where a node label genuinely overlaps the card — the hit lands inside the report, not the label. (2) **App refresh** — vendor colours that mean something (NVIDIA/Apple/AMD), one interaction gradient, device cards with vendor bars, refined inspector/palette/runbar/postmortem/event-console, cursor-tracking glow, reduced-motion support. (3) **Site v2** — aurora hero backdrop, device marquee built from the *real* fitted anchors, asymmetric bento with per-card accents, 3D-tilted product shots, connect-flow explainer, refreshed docs with callouts. (4) **Secure AWS connect is now a real UI**, not just an API: the inspector's AWS ACCOUNT panel generates the per-user ExternalId, deep-links CloudFormation quick-create with the template URL + params prefilled, takes the DeployRoleArn, and drives connect → provision → live status → teardown. **Verified live against our own account**: UI → API → STS assume-role returned `account 703651068111 · ap-south-1 · gpu quota 4 vCPU · 24h session` (no stack provisioned, zero cost). Template also hosted publicly for the one-click path (`s3://clusterbreak-templates-703651068111`). (5) **Verification**: 21/21 UI checks (overlay proof included), 21/21 site checks, audit 0 blockers (the two remaining items are WCAG's inline-link exception, documented), site grader **22/22 with 5/5 controls**. Three grader/audit self-bugs found and fixed in-run: the audit ignored rgba alpha (faking contrast failures), treated clipped decorative layers as overflow (now tests real scrollability), and the old suite still matched v1 markup.
- **~01:00 (Sep 20)** — **Custom domain live: `https://clusterbreak.langersword.in`**. Chain: ACM cert in **us-east-1** (CloudFront requirement; a cert in ap-south-1 is silently unusable) → DNS validation CNAME → **Cloudflare** (zone `44e47a40e0c462aa4d2f4e3ca28991ee`, records DNS-only/grey-cloud so CloudFront keeps TLS) → CloudFront alias + `TLSv1.2_2021` → `clusterbreak` CNAME to the distribution. Cert issued ~1 min after the record landed; distribution redeployed in ~2 min. All three pages serve 200 on the new domain with valid TLS. Canonical + `og:url` tags added so links unfurl with the right host; legacy CloudFront domain keeps working (nothing breaks). **Verified from the new origin**: 21/21 site checks, share flow end-to-end (`https://clusterbreak.langersword.in/app.html?report=pnvtca83yo` renders the postmortem with zero console errors), visual audit 0 blockers. Site grader re-pointed at the canonical domain and extended: **22/22, controls 5/5** (new: canonical-tag drift control). One grader self-bug caught and fixed in-run (dict-key typo would have KeyError'd; caught before trusting green).
- **~00:30 (Sep 20)** — **Production site** (landing + docs): the app moved to `/app.html`; `/` is now a marketing landing and `/docs.html` is real documentation, all per `DESIGN.md` (the design-system contract written before any component code, per the frontend workflow). The hero's signature element is a **live instrument** running the actual sim engine — pick device + model, get real tok/s/fit/KV-wall computed in the browser; nothing on the page is a mock number (verified: 103.5 tok/s RTX 3090 + Llama-8B, reacts to device switches). Real product screenshots captured from the live app (board, postmortem, verdict card). Legacy links (`/?report=`, `/?preset=`) forward to `/app.html` — verified. Visual QA at 1440/768/375 caught real defects: docs tables overflowed at 375px (fixed with scroll wrappers), `--dim-2` failed AA contrast at small sizes (3.35:1 → amended to `#7a838d` = 5.06:1, contract updated in the same change), small tap targets (fixed). Site grader: **18/18, negative controls 4/4 caught** (hardcoded-stat detector, fake-endpoint, corrupted grade claim, token drift) — it also caught two grader self-bugs (chunk scanning, minifier leading-zero) fixed in-run. 21/21 browser checks, 0 blockers in the audit.
- **~18:30 (Sep 19)** — **Cost incident + auto-teardown (v0.4.0)**: the g5.xlarge provisioned for the real-deploy verification on Sep 18 ran unattended for **23 hours** while the session was interrupted — ~$28 of GPU time (at $1.208/hr), $15.31 posted to billing so far, covered by credits. Root cause: no teardown guardrail, and the budget alert ($10) had **no notification subscribers**, so nothing warned. Fixes shipped: (1) all resources verified deleted (instances, volumes, NAT, EIPs — empty); (2) **auto-teardown is now a product feature**: every provision stores `teardownAt` (default 6h) and an **EventBridge rule sweeps every 15 min**, assuming the user's connect role and deleting expired stacks — tested live (`{"ok": true, "swept": 0}`); (3) sweep IAM scoped to the sessions table only. Honest note for the writeup: this is exactly the failure mode the deploy kit must prevent for users — the product now does.
- **~18:25 (Sep 19)** — **Real AWS provisioning proven end-to-end**: connect stack deployed in the user's account → `POST /aws/connect` assumed the role via STS (ExternalId enforced; wrong ExternalId → rejected) → `POST /aws/provision` created a real g5.xlarge rig (CREATE_COMPLETE) → llama.cpp bootstrap debugged live (found: nouveau driver conflict → fixed template: blacklist nouveau + reboot + systemd unit that waits for the GPU; SSM `GetParameters` plural; `ec2:Describe*`; `ModifySubnetAttribute`) → stack torn down, everything verified deleted.

> **Clock note:** this machine's system clock resynced backwards by ~4h45m during the session (reading 10:42, then 05:53). Entries above record the clock as read at the time; durations between entries are the reliable measure. Deadline estimate from the event site's countdown is unaffected in substance (Sunday close, exact time still being finalised).


## Learnings (kept for the writeup)

- **`NODE_ENV=production` in this shell silently skips npm devDependencies** — `npm install` reports "up to date" while `node_modules/.bin` stays empty; `--include=dev` fixes it. Cost ~10 min at the start of the day.
- **API Gateway quick-create does not attach the Lambda permission** — the endpoint 500s until `lambda:add-permission` is added by hand.
- **Fresh `execute-api` hostnames + campus DNS = intermittent resolution failure** — `curl --resolve` bypasses it; works fine once propagated.
- **Fit-threshold recalibration**: first-pass "tight fit" rule called a 138GB model on 192GB unified memory "comfortable"; the researched verdict (30GB headroom is *not* enough for KV growth) forced a stricter 20%-of-usable rule. Caught by the test suite, fixed in the engine.
- **Negative zero is real**: `Math.round(-0.4)` returns `-0`, which deep-equality assertions and cell keys can trip on; grid cells normalize it.
- **The automation browser runs with `--disable-gpu`** (no WebGL, R3F silently can't render); verifying 3D pages needs a separate headless run with software GL (`--use-angle=swiftshader`).
- **UI layout shifts invalidate cached coordinates in scripted browser tests**: the run bar appearing pushes the canvas down, so element positions captured before it must be re-read after the shift (this bit the first break-it verification run).
- **The Hugging Face API (`?blobs=true`) returns exact GGUF blob sizes** — no size in this project is hand-entered any more; hand-entered numbers are how a 20.5 GB "estimate" survived until a live check said 22.29 GB.
- **Measured benchmark tables need defensive parsing**: bold markdown (`**144.49**`) and non-ASCII dashes (U+2011 in "32‑Core") silently dropped rows until normalized.
- **A single default efficiency constant is wrong by construction**: fitted values span 0.36 (H100 PCIe on an 8B model) to 0.83 (RTX 4000 Ada) — fitting per device from measured runs is the honest minimum.
