{
  "name": "@clusterbreak/sim",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Clusterbreak simulation engine — pure TypeScript, deterministic, unit-tested against cited real-world measurements.",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
